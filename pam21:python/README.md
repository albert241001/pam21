# PAM
```
docker run --rm --privileged --name pam.edt.org -h pam.edt.org --network 2hisix -it albert241001/pam21:python /bin/bash

docker run --rm --name ldap.edt.org -h ldap.edt.org --network 2hisix -d edtasixm06/ldap20:group 
```

